<% content_for :title, "#{@app.name} — #{@workspace.name} — Reviews Analytics" %>

<main class="page-container">
  <header class="page-header">
    <h1 class="page-title"><%= @app.name %></h1>
  </header>

  <div class="page-body">
    <p class="app-form-subheading">App created successfully.</p>
  </div>
</main>
