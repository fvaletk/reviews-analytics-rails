<% content_for :title, "Edit #{@app.name} — #{@workspace.name} — Reviewly" %>

<main class="page-container">
  <header class="page-header">
    <h1 class="page-title">Edit App</h1>
    <p class="app-form-subheading">Workspace: <strong><%= @workspace.name %></strong></p>
  </header>

  <div class="page-body">
    <%= render "form" %>
  </div>
</main>
